PORT=8000
CLIENT_URL=http://localhost:3000
MONGODB_URI=your-mongodb-connection-string

# Optional — only needed for Google login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Needed for the AI itinerary generation / recommendation / chat endpoints.
# Free tier: get one at https://console.groq.com/keys (no credit card
# required — 30 req/min, 1000 req/day on Llama 3.3 70B at time of writing,
# see the README for current limits).
GROQ_API_KEY=
